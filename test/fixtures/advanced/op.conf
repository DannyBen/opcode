# Comments are ignored
# greet: even if they look like a command

# Also note this file does NOT end with a newline, and the command reverse
# should still be read properly
greet: echo Hello
reverse: echo $2 and $1